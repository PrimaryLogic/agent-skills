# Validation

Use the skills-ref validator against the skill root:

```bash
skills-ref validate ./primary-logic-external-api
```

Validation checks include:
- frontmatter presence and field validity
- skill name constraints and directory-name alignment
- markdown readability and structure
